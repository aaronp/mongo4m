pipelines.mongo {
  user: "serviceUser"
  password: "changeTh1sDefaultPasswrd"
  uri: "mongodb://localhost:9010"
  database: "dockerenv"
}

pipelines.mongo.databases.audit.pollFrequency : 100ms

pipelines.users.loginHandler: "pipelines.users.mongo.LoginHandlerMongo"
